import './Header.scss';


/**
 * @component Header of the page.
 * @returns {React.ReactElement}
 */
export const Header = () => {
    return (
        <header data-testid='header'>
            <h2>Podcaster</h2>
        </header>
    );
};
